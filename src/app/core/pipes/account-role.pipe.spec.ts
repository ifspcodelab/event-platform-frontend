import { AccountRolePipe } from './account-role.pipe';

describe('AccountRolePipe', () => {
  it('create an instance', () => {
    const pipe = new AccountRolePipe();
    expect(pipe).toBeTruthy();
  });
});
