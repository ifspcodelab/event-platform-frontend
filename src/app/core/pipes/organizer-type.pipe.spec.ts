import { OrganizerTypePipe } from './organizer-type.pipe';

describe('OrganizerTypePipe', () => {
  it('create an instance', () => {
    const pipe = new OrganizerTypePipe();
    expect(pipe).toBeTruthy();
  });
});
