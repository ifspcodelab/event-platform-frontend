import { UsersRolePipe } from './users-role.pipe';

describe('UsersRolePipe', () => {
  it('create an instance', () => {
    const pipe = new UsersRolePipe();
    expect(pipe).toBeTruthy();
  });
});
