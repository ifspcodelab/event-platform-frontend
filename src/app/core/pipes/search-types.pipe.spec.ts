import { SearchTypesPipe } from './search-types.pipe';

describe('SearchTypesPipe', () => {
  it('create an instance', () => {
    const pipe = new SearchTypesPipe();
    expect(pipe).toBeTruthy();
  });
});
