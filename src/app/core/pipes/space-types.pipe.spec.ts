import { SpaceTypesPipe } from './space-types.pipe';

describe('SpaceTypesPipe', () => {
  it('create an instance', () => {
    const pipe = new SpaceTypesPipe();
    expect(pipe).toBeTruthy();
  });
});
